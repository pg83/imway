/* The same fullscreen red dumb-buffer candidate as the scanout taint
 * client; the scenario swaps the display under it and watches the
 * compositor offer it the new mode's size. */
#include "client_kms_scanout_taint.c"
